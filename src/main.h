#include <Arduino.h>

// put function declarations here:
int blink_led(void);
int rgb_led(int red, int green, int blue);

#define LED_PIN_BLINK (17) // Define the pin number for the LED

#define LED_PIN_RED (21) // Define the pin number for the LED
#define LED_PIN_GREEN (22) // Define the pin number for the LED
#define LED_PIN_BLUE (23) // Define the pin number for the LED